/**
 * Enum for web hosts.
 *
 * @author jillurquddus
 * @since  0.0.1
 */

const hosts = {
    'apache-http-server': 'apache/.htaccess', 
    'cloudflare-pages': 'cloudflare/pages/_headers'
};

export default hosts;
